spring:
  profiles:
    active: local

---

spring:
  config:
    activate:
      on-profile: local

  data:
    mongodb:
      host: localhost
      port: 35001
      database: example-stream
      authentication-database: admin